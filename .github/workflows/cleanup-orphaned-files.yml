name: Cleanup Orphaned Files

on:
  schedule:
    # 每周日凌晨2点执行清理
    - cron: '0 2 * * 0'
  workflow_dispatch:  # 允许手动触发

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests

    - name: Run cleanup in dry-run mode
      id: dry-run
      env:
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
      run: |
        python scripts/cleanup_orphaned_files.py

    - name: Upload dry-run report
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: cleanup-dryrun-report
        path: cleanup_report_*.json
      continue-on-error: true  # 即使没有报告文件也不失败

    - name: Check if auto-cleanup should run
      id: check-auto
      run: |
        # 检查是否有孤儿文件需要清理
        if ls cleanup_report_*.json 1> /dev/null 2>&1; then
          report_file=$(ls cleanup_report_*.json | head -1)
          orphaned_count=$(python -c "
          import json
          import glob
          import os
          reports = glob.glob('cleanup_report_*.json')
          if reports and os.path.exists(reports[0]):
              with open(reports[0]) as f:
                  data = json.load(f)
                  print(data.get('statistics', {}).get('orphaned_files', 0))
          else:
              print(0)
          ")

          echo "has_report=true" >> $GITHUB_OUTPUT
          echo "orphaned_count=$orphaned_count" >> $GITHUB_OUTPUT

          if [ "$orphaned_count" -gt 0 ]; then
            echo "has_orphaned=true" >> $GITHUB_OUTPUT
          fi
        else
          echo "No cleanup report generated - no orphaned files found"
          echo "has_report=false" >> $GITHUB_OUTPUT
        fi

    - name: Create issue for manual review
      if: steps.check-auto.outputs.has_orphaned == 'true'
      uses: actions/github-script@v6
      with:
        script: |
          const fs = require('fs');
          const reportFiles = fs.readdirSync('.').filter(f => f.startsWith('cleanup_report_') && f.endsWith('.json'));

          if (reportFiles.length > 0) {
            const reportData = JSON.parse(fs.readFileSync(reportFiles[0], 'utf8'));
            const orphanedCount = reportData.statistics.orphaned_files;

            const issueTitle = `🧹 Orphaned Blueprint Groups Detected (${orphanedCount} groups)`;
            const issueBody = `
## 清理报告

**生成时间**: ${reportData.cleanup_timestamp}
**扫描蓝图组总数**: ${reportData.statistics.total_files_scanned}
**有效蓝图组**: ${reportData.statistics.valid_files}
**孤儿蓝图组**: ${reportData.statistics.orphaned_files}

## 清理策略说明

默认使用**安全移动策略**：
- 📦 移动到 \`.cleanup\` 目录而不是直接删除
- 🔄 自动重新生成 index.json
- 📋 保留Git历史记录
- ⚠️ 可以从 \`.cleanup\` 目录恢复

## 孤儿蓝图组列表

${reportData.orphaned_files.map((group, i) =>
  `${i + 1}. \`${group.blueprint_id}\` - ${Object.values(group).filter(v => v !== group.blueprint_id && v).length} 个文件`
).join('\n')}

## 处理选项

1. **自动移动**: 使用安全策略移动到.cleanup目录
2. **手动处理**: 运行手动清理工作流程
3. **检查后处理**: 确认无误后再清理

---

*此报告由定期清理任务自动生成 | 使用 \`--auto-delete\` 选项进行自动清理*
            `;

            // 查找是否已存在相关issue
            const { data: issues } = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: ['orphaned-blueprints']
            });

            if (issues.length === 0) {
              // 创建新issue
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: issueTitle,
                body: issueBody,
                labels: ['orphaned-blueprints', 'maintenance']
              });
            } else {
              // 更新现有issue
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issues[0].number,
                body: issueBody
              });
            }
          }

